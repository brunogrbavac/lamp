{
    "name": "PHP & MySQL",
    "dockerFile": "../Dockerfile",
    "forwardPorts": [
        80,
        3306
    ],
    "extensions": [
        "felixfbecker.php-pack"
    ],
    "postCreateCommand": "service mariadb start && mariadb -u root < /grant-privileges.sql && service apache2 start && php ./seed.php"
}